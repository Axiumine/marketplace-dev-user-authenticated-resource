// The computed assignment, which carries a `left.property.value` where the plain one has a `left.property.name`.
export const disown = (user: Record<string, unknown>) => {
	user['disabledBy'] = null
}
