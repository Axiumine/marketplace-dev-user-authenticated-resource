// The spread hides the count from the reader and from any selector keyed on `arguments.length`. `unlink`
// rather than `del` because UNLINK is the same command with a different reclaim strategy, and the cluster
// refuses it across slots for exactly the same reason.
export const revoke = async (redisClient, keys) => {
	await redisClient.unlink(...keys)
}
