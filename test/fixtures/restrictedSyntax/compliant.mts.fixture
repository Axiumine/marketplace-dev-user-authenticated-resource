// The shape the nine services actually carry. Nothing here may be reported.
import { sentryBeforeSend } from '@axiumine/marketplace-common/others/sentryBeforeSend'

export const initOptions = {
	dsn: process.env.DSN,
	environment: process.env.NODE_ENV ?? 'unknown',
	integrations: [httpIntegration({ maxIncomingRequestBodySize: 'none' })],
	dataCollection: { userInfo: false, httpBodies: [], stackFrameVariables: false },
	beforeSend: sentryBeforeSend,
	beforeSendTransaction: sentryBeforeSend
}
